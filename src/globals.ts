export enum Suit {
    Diamond,
    Club,
    Heart,
    Spade
}

export enum GameState {
    Start,
    Betting,
    Calling,
    Placing,
    HandEnd,
    End,
}

export enum Team {
    Pro,
    Anti,
}
